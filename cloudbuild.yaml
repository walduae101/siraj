steps:
  # 1a. Validate next.config.mjs contract before build
  - name: 'gcr.io/cloud-builders/bash'
    id: validate-config
    entrypoint: bash
    args:
      - -lc
      - |
        set -euo pipefail
        echo "🔍 Validating next.config.mjs header contract..."
        chmod +x scripts/validate-config.sh || true
        ./scripts/validate-config.sh
        echo "✅ Config validation passed"

  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/siraj:$COMMIT_SHA', '.']

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/siraj:$COMMIT_SHA']

  # Deploy to Cloud Run (US region)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args:
      - run
      - deploy
      - siraj
      - --image
      - gcr.io/$PROJECT_ID/siraj:$COMMIT_SHA
      - --region
      - us-central1
      - --platform
      - managed
      - --allow-unauthenticated
      - --set-env-vars
      - NEXT_PUBLIC_FIREBASE_API_KEY=${_NEXT_PUBLIC_FIREBASE_API_KEY},NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${_NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN},NEXT_PUBLIC_FIREBASE_PROJECT_ID=${_NEXT_PUBLIC_FIREBASE_PROJECT_ID},NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${_NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET},NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${_NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID},NEXT_PUBLIC_FIREBASE_APP_ID=${_NEXT_PUBLIC_FIREBASE_APP_ID},NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=${_NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID}

  # 1b. Capture the exact image the US service is running
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    id: capture-us-image
    entrypoint: bash
    args:
      - -lc
      - |
        set -euo pipefail
        echo "📸 Capturing US image digest..."
        IMAGE="$(gcloud run services describe siraj --region=us-central1 \
          --format='value(spec.template.spec.containers[0].image)')"
        echo "IMAGE=${IMAGE}" | tee image.env
        echo "✅ US image: ${IMAGE}"

  # 1c. Deploy EU to the exact same image (parity)
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    id: deploy-eu-parity
    entrypoint: bash
    args:
      - -lc
      - |
        set -euo pipefail
        source image.env
        echo "🌍 Deploying EU to same image: ${IMAGE}"
        gcloud run services update siraj-eu --region=europe-west1 --image "$IMAGE" --quiet
        echo "✅ EU deployment complete"

  # 1d. Invalidate CDN cache
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    id: invalidate-cdn
    entrypoint: bash
    args:
      - -lc
      - |
        set -euo pipefail
        echo "🔄 Invalidating CDN cache..."
        gcloud compute url-maps invalidate-cdn-cache siraj-web-map --path '/*' --quiet
        echo "✅ CDN cache invalidated"

  # 1e. Post-deploy verifications (origin parity + CDN headers)
  - name: 'gcr.io/cloud-builders/bash'
    id: verify-origins
    entrypoint: bash
    args:
      - -lc
      - |
        set -euo pipefail
        echo "🔍 Verifying origin parity..."
        chmod +x scripts/verify-origins.sh || true
        ./scripts/verify-origins.sh
        echo "✅ Origin parity verified"

  - name: 'gcr.io/cloud-builders/bash'
    id: cdn-parity
    entrypoint: bash
    args:
      - -lc
      - |
        set -euo pipefail
        echo "🌐 Verifying CDN headers..."
        chmod +x scripts/cdn-parity.sh || true
        ./scripts/cdn-parity.sh
        echo "✅ CDN headers verified"

  # 1f. Quick smoke test (fast headers check)
  - name: 'gcr.io/cloud-builders/bash'
    id: quick-check
    entrypoint: bash
    args:
      - -lc
      - |
        set -euo pipefail
        echo "⚡ Running quick smoke test..."
        chmod +x scripts/quick-check.sh || true
        ./scripts/quick-check.sh
        echo "✅ Quick check passed"

  # 1g. tRPC post-deploy checks (prod)
  - id: "tRPC post-deploy checks (prod)"
    name: node:20
    entrypoint: bash
    args:
      - -lc
      - |
        node -v
        node .github/scripts/trpc-check.mjs
    env:
      - BASE_URL=https://siraj.life
      - MAX_ATTEMPTS=30
      - SLEEP_SECONDS=20
    waitFor:
      - "-"   # or the id of your previous "Invalidate CDN" step

substitutions:
  _URL_MAP: siraj-web-map
  _SERVICE_US: siraj
  _SERVICE_EU: siraj-eu
  _REGION_US: us-central1
  _REGION_EU: europe-west1

images:
  - 'gcr.io/$PROJECT_ID/siraj:$COMMIT_SHA'
